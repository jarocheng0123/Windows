# Windows 系统配置与重装指南

## 目录
- [BIOS/UEFI 配置](#biosuefi-配置)
- [系统分区规划](#系统分区规划)
- [系统重装流程](#系统重装流程)
- [系统设置优化](#系统设置优化)
- [BIOS/UEFI 与 MBR/GPT 详解](#biosuefi-与-mbrgpt-详解)
- [启动设备说明](#启动设备说明)
- [系统镜像格式](#系统镜像格式)

---

## BIOS/UEFI 配置

### 如何进入 BIOS/UEFI 设置
- **开机时按键**：开机后立即连续按 `F2`、`F12`、`Del`、`Esc` 等键（不同品牌按键不同，常见为 `F2` 或 `Del`）
- **Windows 系统内进入**：
  - Win10/11：设置 → 更新和安全 → 恢复 → 高级启动 → 立即重新启动 → 疑难解答 → 高级选项 → UEFI 固件设置
  - 或按住 `Shift` 键点击"重启"，进入高级启动选项

### 核心设置项
- **CPU 虚拟化（Intel VT-x / AMD-V）**：
  - 位置：Advanced → CPU Configuration → Virtualization Technology（Intel）或 SVM Mode（AMD）
  - 作用：启用后支持虚拟机（VMware、VirtualBox）、Windows 子系统 for Android（WSA）等
  - 建议：**必须启用**（即使不用虚拟机，WSA 也需要）

- **安全启动（Secure Boot）**：
  - 位置：Boot → Secure Boot
  - Win11：**必须启用**（系统强制要求）
  - Win10：可选，但建议启用（提高安全性，防止恶意软件篡改引导）
  - 注意：启用 Secure Boot 后，部分 Linux 发行版和第三方系统可能无法启动

- **开盖启动（仅笔记本）**：
  - 位置：Power Management → Lid Open Action
  - 选项：开机/唤醒/无操作
  - 根据个人使用习惯设置

- **引导模式（Boot Mode）**：
  - 位置：Boot → Boot Mode 或 Boot List Option
  - **UEFI**：新电脑（2012年后）推荐，支持 GPT 分区、快速启动、Secure Boot
  - **Legacy/BIOS**：老电脑（2011年前）或需要兼容老系统时使用，仅支持 MBR 分区
  - 注意：选择后需与硬盘分区表匹配（UEFI 配 GPT，BIOS 配 MBR）

### 引导配置
- **启动顺序（Boot Priority）**：
  - 位置：Boot → Boot Priority Order
  - 正常使用：优先从 HDD/SSD（硬盘）启动
  - 重装系统：临时将 USB 设为第一启动项，装完后改回 HDD

- **快速启动（Fast Boot）**：
  - 位置：Boot → Fast Boot
  - 作用：跳过部分硬件自检，加快开机速度（约快 5-10 秒）
  - 建议：**启用**（不影响系统稳定性）

- **TPM 2.0（可信平台模块）**：
  - 位置：Security → TPM Configuration 或 Advanced → Trusted Computing
  - Win11：**必须启用并设置为 2.0 版本**（系统强制要求）
  - Win10：可选，但启用后可支持 BitLocker 加密、Windows Hello 等功能
  - 注意：部分老主板可能不支持 TPM 2.0，需升级 BIOS 或使用 TPM 模块

---

## 系统分区规划

### 推荐分区方案（UEFI+GPT，总容量约 930GB）

| 分区 | 盘符 | 容量 | 用途说明 |
|------|------|------|----------|
| **ESP 分区** | - | 260MB | 引导分区（UEFI 必需，系统自动创建，存储引导文件） |
| **MSR 分区** | - | 128MB | 系统预留分区（系统自动创建，支持 BitLocker 等功能） |
| **系统盘** | C: | 300GB | Windows 系统、用户文件夹（C:\Users）、系统缓存 |
| **软件盘** | D: | 300GB | 软件安装路径<br>• 在 D 盘创建 `Program Files` 和 `Program Files (x86)` 目录<br>• 安装软件：TIA、Fanuc、2345 破解版、迅雷破解版等<br>• **优势**：避免 C 盘安装请求权限问题，便于管理 |
| **资源库** | E: | 200GB | 存储固定资源<br>• 安装包/镜像文件<br>• 文件夹背景图（固定文件）<br>• VMware 打包镜像<br>• IDM 配置文件<br>• 激活文件<br>• VM 限制解锁工具<br>• VMtools<br>• PE 软件 |
| **数据盘** | F: | 130GB | 用户数据<br>• 文件/共享文件夹<br>• VMware 虚拟机文件 |

### 分区注意事项
- **ESP 和 MSR 分区**：系统安装时自动创建，切勿删除（删除后无法启动）
- **C 盘容量**：建议至少 100GB，推荐 200-300GB（系统+常用软件+缓存）
- **D 盘模拟默认路径**：创建 `Program Files` 和 `Program Files (x86)` 可避免部分软件安装路径错误
- **4K 对齐**：使用 SSD 时，分区需 4K 对齐（现代分区工具默认对齐，无需手动设置）
- **分区顺序**：建议按 C、D、E、F 顺序创建，便于管理

---

## 系统重装流程

### 系统版本选择
- **推荐版本**：Windows 10/11 专业工作站版（Professional Workstation）
- **国家/地区**：选择美国或欧盟（避免区域限制）
- **用户名**：**不要使用中文名**（避免路径问题）

### 重装前准备
1. **备份重要数据**：文档、照片、配置文件等
2. **准备系统镜像**：
   - Windows 镜像（ISO 格式）
   - PE 镜像（用于系统维护）
3. **准备驱动**：网卡驱动、主板驱动（重装后可能需要）

### 重装步骤

#### 1. 制作启动盘
- **工具选择**：
  - **Rufus**：推荐，支持 UEFI/BIOS 双模式，兼容性好
  - **MediaCreationTool**：微软官方工具，仅支持 Windows 系统
  - **UltraISO**：老牌工具，功能全面
- **操作步骤**：
  1. 下载 Windows ISO 镜像（从微软官网或 MSDN）
  2. 插入 U盘（容量 ≥ 8GB，建议使用 USB 3.0）
  3. 打开 Rufus，选择 ISO 文件
  4. 分区类型选择：**GPT（UEFI）** 或 **MBR（BIOS）**（需与主板启动模式匹配）
  5. 文件系统选择：**FAT32**（UEFI）或 **NTFS**（BIOS）
  6. 点击"开始"，等待写入完成

#### 2. BIOS/UEFI 设置
- 进入 BIOS/UEFI（方法见上文）
- 设置启动顺序：将 USB 设备设为第一启动项
- 确认启动模式：UEFI 模式需与 GPT 分区匹配，BIOS 模式需与 MBR 分区匹配
- 保存并退出（通常按 `F10`）

#### 3. 安装系统
- 从 U盘启动后，进入 Windows 安装界面
- **选择语言和区域**：建议选择美国或欧盟（避免区域限制）
- **选择安装类型**：自定义安装（高级）
- **分区操作**：
  - 删除旧分区（注意：会清空所有数据，务必提前备份）
  - 选择未分配空间，点击"新建"
  - 系统会自动创建 ESP（260MB）和 MSR（128MB）分区
  - 创建系统分区（C盘，建议 200-300GB）
  - 创建其他数据分区（D、E、F 盘）
- **安装过程**：等待系统文件复制和安装（约 20-30 分钟）

#### 4. 首次启动配置（OOBE）
- **跳过联网**：在"让我们为你连接到网络"界面，按 `Shift + F10` 打开命令提示符，输入 `oobe\bypassnro` 回车，系统会重启并跳过联网
- **创建本地账户**：
  - 用户名：**不要使用中文名**（避免路径问题，如 `C:\Users\张三` 可能导致部分软件异常）
  - 密码：可选，建议设置（提高安全性）
- **隐私设置**：关闭所有隐私选项（位置、诊断数据、广告 ID 等）
- **Cortana**：可选择跳过（Win10）或禁用（Win11）

---

## 系统设置优化

### 文件夹与路径配置

#### 用户文件夹位置
- **C:\Users 固定**：用户文件夹默认在 C 盘，不建议移动（可能导致部分软件异常）
- **如需移动**：右键用户文件夹 → 属性 → 位置 → 移动（需谨慎操作）

#### 常用路径
- **开机自启文件夹**：
  - 路径：`C:\Users\[用户名]\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup`
  - 快捷方式：按 `Win + R`，输入 `shell:startup` 回车
  - 用途：将程序快捷方式放入此文件夹即可开机自启

- **Clash Verge 配置路径**：
  - 路径：`C:\Users\[用户名]\.config\clash-verge` 或安装目录下的 `config` 文件夹
  - 用途：存放代理配置文件、规则文件等

- **苹果设备备份路径**：
  - 路径：`C:\Users\[用户名]\AppData\Roaming\Apple Computer\MobileSync\Backup`
  - 用途：iTunes/访达备份 iPhone/iPad 数据的位置

- **浏览器垂直标签**：
  - Edge：设置 → 外观 → 垂直标签栏（启用）
  - Chrome：需安装扩展（如"Sidewise"）或使用 Edge

### 系统功能配置

#### 系统激活
- **方法 1**：使用数字许可证（已激活的电脑重装后自动激活）
- **方法 2**：使用激活工具（如 KMS、HWID 等，需自行查找）
- **方法 3**：使用正版密钥（从微软官网购买）

#### 驱动安装
- **推荐顺序**：
  1. 主板驱动（芯片组、USB、SATA）
  2. 网卡驱动（确保能联网）
  3. 显卡驱动（NVIDIA/AMD/Intel）
  4. 声卡、蓝牙、WiFi 等
- **工具选择**：
  - **官网驱动**：最稳定，推荐优先使用
  - **驱动精灵/驱动人生**：自动识别硬件，但可能包含广告
  - **Windows Update**：系统会自动安装部分驱动

#### 系统更新
- **Windows Update**：
  - 设置 → 更新和安全 → Windows Update
  - 建议：启用自动更新（至少安全更新）
- **Microsoft Store 更新**：
  - 打开 Microsoft Store → 库 → 获取更新

#### 关闭 BitLocker
- **位置**：控制面板 → 系统和安全 → BitLocker 驱动器加密
- **操作**：对已加密的驱动器点击"关闭 BitLocker"
- **注意**：解密过程可能需要较长时间

#### 传递优化（Delivery Optimization）
- **位置**：设置 → 更新和安全 → 传递优化
- **建议设置**：
  - 关闭"允许从其他电脑下载"（节省带宽）
  - 或限制下载/上传带宽

#### Windows Defender 配置
- **关闭实时扫描**（如使用第三方杀毒）：
  - 设置 → 更新和安全 → Windows 安全中心 → 病毒和威胁防护
  - 管理设置 → 关闭"实时保护"
- **关闭安全中心通知**：
  - 设置 → 系统 → 通知和操作 → 关闭"Windows 安全中心"通知
- **关闭防火墙**（如使用第三方防火墙）：
  - 设置 → 更新和安全 → Windows 安全中心 → 防火墙和网络保护
  - 关闭所有网络的防火墙

### 用户体验优化

#### UAC（用户账户控制）
- **位置**：控制面板 → 系统和安全 → 更改用户账户控制设置
- **建议**：降低到"仅在应用尝试更改我的计算机时通知我"（第二档）
- **注意**：不建议完全关闭（降低安全性）

#### 关闭系统广告
- **Windows 聚焦锁屏**：
  - 设置 → 个性化 → 锁屏界面 → 背景选择"图片"或"幻灯片放映"
- **开始菜单推荐**：
  - 设置 → 个性化 → 开始 → 关闭"显示最近添加的应用"和"显示最常用的应用"
- **任务栏资讯**：
  - 右键任务栏 → 资讯和兴趣 → 关闭

#### 浏览器广告拦截
- **Edge/Chrome**：安装 uBlock Origin 扩展
- **Firefox**：内置跟踪保护，或安装 uBlock Origin

#### 电源管理
- **电源键行为**：
  - 设置 → 系统 → 电源和睡眠 → 其他电源设置 → 选择电源按钮的功能
- **开合盖行为**（笔记本）：
  - 同上位置 → 选择关闭盖子时执行的操作
- **睡眠模式**：
  - 设置 → 系统 → 电源和睡眠 → 设置睡眠时间
- **快速启动**：
  - 控制面板 → 电源选项 → 选择电源按钮的功能 → 启用"快速启动"（推荐）

### 系统组件安装

#### 启用 Windows 功能
- **路径**：控制面板 → 程序 → 启用或关闭 Windows 功能
- **.NET Framework 3.5**：
  - 勾选启用（部分老软件需要，如某些工业软件）
  - 如无法启用，需联网下载或使用安装介质
- **Windows 子系统 for Linux（WSL）**：
  - 勾选启用（如需运行 Linux 环境）
- **Windows 子系统 for Android（WSA）**：
  - Win11 专用，需从 Microsoft Store 安装
  - 前提：启用虚拟化功能（见 BIOS 设置）

### 辅助功能

#### 鼠标指针位置
- **位置**：设置 → 辅助功能 → 鼠标指针和触摸
- **功能**：当按住 `Ctrl` 键时显示鼠标指针位置（便于定位）
- **其他设置**：可调整指针大小、颜色等

---










## BIOS/UEFI 与 MBR/GPT 详解

在配置 BIOS/UEFI 和规划分区之前，需要深入理解 BIOS/UEFI 与 MBR/GPT 的关系。本节从**核心区别**和**强制搭配**两方面梳理，兼顾技术原理与实操性，帮助您做出正确的选择。

> **前置知识**：如果您还不熟悉如何进入 BIOS/UEFI 设置，请先阅读[BIOS/UEFI 配置](#biosuefi-配置)章节。

### 核心区别与强制搭配指南

#### 一、BIOS vs UEFI（启动"桥梁"的区别）

BIOS 和 UEFI 是电脑开机时"连接硬件与系统"的核心程序，本质是**不同时代的启动标准**，决定了电脑能识别哪种分区表、支持哪些功能。

**如何检查当前系统是 UEFI 还是 BIOS？**
- **方法 1**：按 `Win + R`，输入 `msinfo32`，查看"BIOS 模式"（显示"UEFI"或"传统"）
- **方法 2**：按 `Win + R`，输入 `diskpart`，输入 `list disk`，查看 GPT 列是否有星号（*）
  - 有星号 = GPT 分区 = UEFI 启动
  - 无星号 = MBR 分区 = BIOS 启动
- **方法 3**：查看是否有 EFI 文件夹（`C:\Windows\System32\` 下或 ESP 分区中）

| 对比维度       | BIOS（传统标准）                | UEFI（现代标准）                  |
|----------------|---------------------------------|-----------------------------------|
| 诞生背景       | 1975年随 IBM PC 推出，适配早期硬件 | 2005年 UEFI 2.0 标准发布，2007年后逐步普及，解决 BIOS 的容量/速度瓶颈 |
| 启动流程       | POST自检→读取硬盘**第一个扇区（MBR）** →引导系统 | 硬件初始化→读取**ESP分区（GPT专属）** →引导系统 |
| 支持分区表     | 仅支持MBR（无法识别GPT）        | 主要支持GPT，部分主板可开"Legacy兼容模式"支持MBR |
| 核心限制       | 1. 硬盘最大仅支持2TB；2. 最多4个主分区 | 1. 硬盘容量无上限（理论18EB）；2. 最多128个主分区 |
| 安全功能       | 无Secure Boot（易被恶意引导）   | 支持Secure Boot（Win11强制要求，防非法系统） |
| 操作体验       | 纯文本界面，仅支持键盘          | 图形化界面，支持鼠标，部分带触控       |
| 启动速度       | 慢（串行初始化硬件，通常30-60秒） | 快（并行初始化硬件，支持"快速启动"，通常10-20秒） |
| 兼容系统       | 适配WinXP/Win7等老系统          | 适配Win8及以上新系统（Win11必须UEFI）  |


#### 二、MBR vs GPT（硬盘"分区地图"的区别）
MBR和GPT是硬盘存储“分区信息”的格式，相当于**硬盘的“地图”**，决定了电脑能识别多大硬盘、分多少区，且必须与启动方式（BIOS/UEFI）匹配。

| 对比维度       | MBR（主引导记录）               | GPT（全局唯一标识分区表）         |
|----------------|---------------------------------|-----------------------------------|
| 存储位置       | 硬盘第一个扇区（512字节，易损坏） | 分区表存多处+备份（损坏后可恢复）   |
| 硬盘容量限制   | 最大支持2TB（超2TB部分无法识别） | 无容量限制（支持2TB以上大容量硬盘） |
| 主分区数量     | 最多4个（需扩展分区才能分更多逻辑分区） | 最多128个主分区（无需扩展分区）     |
| 配套分区       | 无专属分区                      | 必须有**ESP分区（FAT32，存引导文件）** 和**MSR分区（预留空间，支持加密等功能）** |
| 数据安全性     | 分区表损坏后难恢复（无备份）    | 分区表有冗余备份，抗损坏能力强       |
| 支持启动方式   | 仅支持BIOS启动                  | 仅支持UEFI启动（核心匹配逻辑）       |
| 适用场景       | 老旧电脑、2TB以内硬盘、Win7及以下系统 | 新电脑、2TB以上硬盘、Win8及以上系统（Win11必选） |


#### 三、关键规则：强制搭配，不能乱组合

电脑启动的核心逻辑是"**启动方式（BIOS/UEFI）必须与分区表（MBR/GPT）一一对应**"，混配会直接导致"找不到系统"或安装失败，具体搭配如下：

##### 1. 唯一合法搭配（必记）

| 启动方式 | 强制对应分区表 | 适用场景（结合实操）                |
|----------|----------------|-------------------------------------|
| BIOS     | MBR            | 老电脑装Win7 32位、2TB以内硬盘存储  |
| UEFI     | GPT            | 新电脑装Win10/11、2TB以上硬盘、双系统（Win+Linux） |

##### 2. 常见误区：为什么不能混配？

- **误区1**："UEFI主板装MBR硬盘"
  - 问题：UEFI 只能读 GPT 的 ESP 分区，找不到 MBR 的引导记录
  - 结果：会提示"无启动设备"或"Operating System not found"
  - 解决：转换为 GPT 分区表（见下方转换方法）

- **误区2**："BIOS主板装GPT硬盘"
  - 问题：BIOS 只能读 MBR 的第一个扇区，读不懂 GPT 的分区信息
  - 结果：同样无法启动
  - 解决：转换为 MBR 分区表，或启用 UEFI 模式

- **误区3**："开UEFI Legacy模式用MBR"
  - 说明：部分新主板支持"Legacy兼容模式"（CSM，可在BIOS中切换）
  - 问题：会丢失 UEFI 的速度和安全优势（如无法开Secure Boot、快速启动）
  - 建议：仅临时救急（如老系统迁移），不推荐长期使用

##### 3. MBR 与 GPT 相互转换

**⚠️ 警告：转换分区表会清空所有数据，务必提前备份！**

**MBR 转 GPT（推荐使用 Windows 安装盘或 PE 工具）**：
1. 使用 Windows 安装盘启动
2. 按 `Shift + F10` 打开命令提示符
3. 输入 `diskpart` → `list disk` → `select disk X`（X为磁盘号）
4. 输入 `clean`（清空磁盘）
5. 输入 `convert gpt`（转换为GPT）
6. 重新分区并安装系统

**GPT 转 MBR**（类似步骤，最后输入 `convert mbr`）

**使用第三方工具**：DiskGenius、AOMEI Partition Assistant 等（支持无损转换，但仍有风险）


#### 四、实操场景：结合之前的搭配表，补充关键细节

| 场景                | 启动方式+分区表 | 核心注意事项（避坑点）                          |
|---------------------|-----------------|---------------------------------------------|
| 新电脑装Win11       | UEFI+GPT        | 必须开"Secure Boot"和"TPM 2.0"（Win11强制要求），ESP分区由系统自动创建（约260MB），建议ESP分区≥512MB（便于后续维护） |
| 老旧电脑装Win7 32位 | BIOS+MBR        | 硬盘必须≤2TB，若存4GB以上大文件，数据分区选NTFS（FAT32存不了大文件），注意Win7不支持GPT |
| 双系统（Win10+Ubuntu） | UEFI+GPT    | 共享1个ESP分区（建议512MB，够存双系统引导文件），先装Win10再装Ubuntu（避免覆盖引导），Ubuntu安装时选择"与Windows共存" |
| 2TB以上硬盘存数据   | UEFI+GPT        | 无需创建ESP/MSR分区（仅存数据，不参与启动），全部分为NTFS分区（兼容Windows），可挂载为从盘使用 |
| 老电脑升级Win10     | BIOS+MBR 或 UEFI+GPT | 如主板支持UEFI，建议转换为UEFI+GPT（获得更快启动速度），否则保持BIOS+MBR |


#### 五、总结：3句话搞定选择
1. 新电脑（2012年后）、装Win10/11、用2TB以上硬盘→ 选**UEFI+GPT**；  
2. 老电脑（2011年前）、装Win7及以下、用2TB以内硬盘→ 选**BIOS+MBR**；  
3. 记住“UEFI配GPT，BIOS配MBR”，混配必出错，别尝试“兼容模式”长期用。




### UEFI+GPT/BIOS+MBR 分区情况、文件系统及删除规则

在理解了 BIOS/UEFI 与 MBR/GPT 的搭配关系后，本节深入讲解两种搭配下的分区结构、文件系统选择以及分区删除的注意事项，帮助您在实际操作中避免误操作导致系统无法启动。

结合两种启动搭配的核心逻辑，从"默认分区""文件系统适配""分区删除规则"三方面拆解，清晰说明各分区的作用与操作边界。

#### 一、两种搭配下的默认分区及文件系统
### 1. UEFI+GPT（新电脑/Win10/11/大硬盘）
UEFI启动依赖GPT分区表的**专属核心分区**，默认会生成3类关键分区（系统安装时自动创建，无需手动干预），文件系统有严格限制：

| 分区名称       | 作用（通俗理解）                  | 文件系统       | 大小建议       | 能否删除？ | 删除影响                     |
|----------------|-----------------------------------|----------------|----------------|------------|------------------------------|
| ESP分区        | 存“启动钥匙”（如Win的引导文件）   | 必须是FAT32    | 100-512MB（Win11默认260MB） | ❌ 不能删  | 电脑找不到启动文件，直接开不了机 |
| MSR分区        | 系统“预留空间”（支持BitLocker加密、磁盘扩展） | 无格式（隐藏） | 128MB（≥16GB硬盘） | ❌ 不能删  | 无法启用BitLocker，磁盘扩展可能失败 |
| 系统分区（C盘）| 装操作系统（Win10/11）            | 推荐NTFS       | 至少100GB      | ❌ 不建议删 | 系统丢失，所有软件/数据清空   |
| 数据分区（D/E盘）| 存文件（文档、视频等）            | 推荐NTFS       | 剩余空间分配   | ✅ 可以删  | 仅丢失该分区内的数据，不影响启动 |
| 品牌机恢复分区 | 存出厂系统镜像（如联想/戴尔的恢复文件） | NTFS（隐藏）   | 10-20GB        | ✅ 可删（慎）| 丢失“一键恢复出厂系统”功能，不影响当前系统 |


### 2. BIOS+MBR（老电脑/Win7/小硬盘≤2TB）
BIOS启动无专属核心分区，分区结构更简单，仅需区分“主分区”和“扩展分区”：

| 分区名称       | 作用（通俗理解）                  | 文件系统       | 大小建议       | 能否删除？ | 删除影响                     |
|----------------|-----------------------------------|----------------|----------------|------------|------------------------------|
| 主分区（C盘）  | 装操作系统（Win7）                | 推荐NTFS       | 至少50GB       | ❌ 不建议删 | 系统丢失，软件/数据清空       |
| 主分区（D盘）  | 存数据（可选，MBR最多4个主分区）  | FAT32/NTFS     | 按需分配       | ✅ 可以删  | 仅丢失该分区数据             |
| 扩展分区       | “容器”：装多个逻辑分区（突破4主分区限制） | 无格式（仅作容器） | 剩余空间       | ✅ 可以删  | 会连带删除扩展分区内所有逻辑分区 |
| 逻辑分区（E/F盘）| 存数据（扩展分区内创建）          | FAT32/NTFS     | 按需分配       | ✅ 可以删  | 仅丢失该逻辑分区数据         |
| 老品牌机恢复分区 | 存Win7出厂镜像（部分机型有）      | NTFS（隐藏）   | 5-15GB         | ✅ 可删（慎）| 丢失“一键恢复”功能           |


#### 二、常见文件系统（FAT16/FAT32/NTFS）区别与适配
不同分区需搭配对应的文件系统，核心是“**功能匹配需求**”，避免格式错误导致无法使用：

| 文件系统 | 核心特点                          | 单文件大小限制 | 分区大小限制  | 适用场景（对应分区）                | 优缺点                     |
|----------|-----------------------------------|----------------|---------------|-------------------------------------|----------------------------|
| FAT16    | 兼容性极老（支持DOS/Win98）       | ≤2GB           | ≤4GB          | 老设备（如老式U盘、工业设备）       | ✅ 兼容老系统；❌ 容量/文件限制大，几乎淘汰 |
| FAT32    | 跨系统兼容（Win/Mac/Linux都能读） | ≤4GB           | ≤32GB（实际） | 1. UEFI的ESP分区（必须用）；2. 小U盘（≤32GB） | ✅ 兼容性强；❌ 存不了大文件（如4GB以上视频） |
| NTFS     | 功能全（支持加密、权限、大文件）  | 接近16EB（无限制） | ≤256TB        | 1. 系统分区（C盘）；2. 数据分区（D/E盘）；3. 恢复分区 | ✅ 支持大文件/加密；❌ Mac/Linux默认只读（需装工具） |
| EXT4     | Linux专属（如Ubuntu）             | 接近16EB       | ≤1EB          | 双系统中Ubuntu的系统/数据分区       | ✅ 适配Linux；❌ Windows默认读不了（需工具） |


#### 三、分区删除的核心规则：能删什么？删了有什么用？
### 1. 能删的分区（安全边界）
只有“**非启动、非系统、非预留**”的分区可删，核心是：  
- 数据分区（D/E/F盘，无论UEFI还是BIOS）；  
- 品牌机恢复分区（不依赖出厂恢复功能时）；  
- BIOS下的扩展分区/逻辑分区（不影响主分区系统时）。

### 2. 不能删的分区（删除即故障）
删了会导致“**启动失败**”或“**核心功能失效**”，绝对不能动：  
- UEFI+GPT的ESP分区、MSR分区；  
- 任何搭配下的“系统分区（C盘）”；  
- BIOS下扩展分区内的“逻辑分区”若关联系统文件（罕见），也不能删。

### 3. 删除分区的作用（为什么要删？）
- **腾空间**：合并小分区（如删D盘，把空间分给C盘）；  
- **重新规划**：重装系统前删所有分区，避免旧分区干扰；  
- **清理无用分区**：删品牌机自带的冗余恢复分区（节省空间）；  
- **修复问题**：删除损坏的逻辑分区（如无法格式化的分区）。

### 4. 删除前必做：规避风险
1. **备份数据**：无论删哪个分区，先把里面的重要文件（照片、文档）拷贝到其他硬盘；  
2. **确认分区类型**：用“磁盘管理”（Win+X打开）确认分区是否为“系统分区/ESP分区”，避免误删；  
3. **慎删隐藏分区**：隐藏分区（如MSR、恢复分区）默认不显示，删前先查用途（品牌机官网可查）。


#### 四、总结：关键记3点
1. **UEFI+GPT必保2个分区**：ESP（FAT32）和MSR，删了开不了机；  
2. **BIOS+MBR注意4主分区限制**：超4个分区需建扩展分区，删扩展分区会丢逻辑分区；  
3. **文件系统匹配需求**：ESP必须FAT32，系统/数据分区用NTFS，小U盘用FAT32。



## 启动设备说明（HDD/FDD）

在启动设置中，**HDD和FDD并非"启动模式"（启动模式是UEFI/BIOS），而是"启动设备类型"**——即电脑开机时优先从哪个硬件读取系统文件。两者是早期到现在常见的存储设备，核心区别在于硬件形态和功能。

> **关联阅读**：启动模式（UEFI/BIOS）的选择已在[BIOS/UEFI 配置](#biosuefi-配置)章节说明，本节重点介绍启动设备的选择。

### 一、启动模式 vs 启动设备

很多人会混淆这两个概念，先简单区分：

- **启动模式**：UEFI/BIOS，是电脑"识别硬件、引导系统"的底层规则（比如UEFI只能读GPT硬盘，BIOS读MBR硬盘）
- **启动设备**：HDD/FDD/USB/CD-ROM等，是"存储系统文件的硬件"（比如系统装在硬盘里，就选HDD启动）

**简单说**：启动模式是"规则"，启动设备是"执行规则的硬件"。

**设置位置**：
- BIOS/UEFI 设置界面 → Boot（启动）→ Boot Priority Order（启动优先级顺序）
- 或 Boot Sequence（启动顺序）


### 二、HDD：硬盘启动（现在最常用）
### 1. 全称与含义
HDD = **Hard Disk Drive**，即“硬盘驱动器”，包括我们现在常用的：
- 机械硬盘（传统HDD，靠磁盘旋转读写）；  
- 固态硬盘（SSD，虽叫SSD，但在启动设备列表里仍常标注为“HDD”，本质都是“硬盘类存储”）。

### 2. 核心作用
电脑开机时，若启动设备选“HDD”，则会从硬盘中读取系统引导文件（比如UEFI+GPT的ESP分区、BIOS+MBR的MBR扇区），进而加载Windows/Linux等系统——**这是现在绝大多数电脑的“默认启动设备”**（因为系统都装在硬盘里）。

### 3. 实操场景
- 正常使用电脑：启动设备设为“HDD”（确保从硬盘启动已安装的系统）；  
- 重装系统后：需确认启动设备仍为“HDD”（避免误选USB/CD-ROM导致反复进入安装界面）；  
- 多硬盘场景：若电脑接了2块硬盘（如SSD+机械硬盘），启动设备列表会显示“硬盘1（SSD）”“硬盘2（机械硬盘）”，选装了系统的那一块（通常是SSD，启动更快）。


### 三、FDD：软盘启动（已淘汰，仅老设备可见）
### 1. 全称与含义
FDD = **Floppy Disk Drive**，即“软盘驱动器”，对应早期的“软盘”（3.5英寸或5.25英寸的软磁盘，容量仅1.44MB，像小型光盘）。

### 2. 核心作用
在2000年以前，软盘是主流移动存储设备，电脑会通过FDD读取软盘里的系统文件（比如早期DOS系统、系统修复工具）——**现在软盘和FDD早已淘汰，新主板（2010年后）基本不再支持FDD接口，启动设备列表里也很少出现FDD选项**。

### 3. 实操场景
- 仅老电脑（如2005年前的台式机）可能在BIOS启动列表里看到FDD；  
- 现在遇到FDD选项：无需理会，直接设为“禁用”或“优先级最低”（避免因找不到软盘导致启动报错）。


### 四、常见启动设备对比：除了HDD/FDD，还有这些
在BIOS/UEFI启动设置里，除了HDD和FDD，还会看到其他设备，按需选择即可：

| 启动设备类型 | 全称/含义                | 核心用途（什么时候选）                  |
|--------------|--------------------------|-----------------------------------------|
| HDD          | 硬盘驱动器               | 日常启动已安装的系统（默认选这个）      |
| FDD          | 软盘驱动器               | 老设备读取软盘（现在基本不用）          |
| USB          | USB存储设备（U盘/移动硬盘） | 重装系统（从U盘启动安装包）            |
| CD-ROM/DVD   | 光盘驱动器               | 从系统光盘启动（现在被U盘替代，较少用） |
| Network      | 网络启动                 | 企业批量装机（通过局域网加载系统）      |


### 五、总结：关键记3点

1. **HDD是现在的核心启动设备**：系统装在硬盘（SSD/机械硬盘）里，开机就选HDD，别选错
2. **FDD是历史产物**：新电脑看不到，老电脑遇到也不用管，优先选HDD或USB
3. **重装系统时的操作**：先把启动设备优先级设为"USB"（从U盘启动安装包），装完后改回"HDD"（从硬盘启动系统）

---

## 系统镜像格式详解

系统镜像格式是重装系统或系统备份时使用的文件格式，不同格式有不同的特点和适用场景。了解各种格式有助于选择合适的工具和方法。








## 系统镜像格式详解

### 重装/恢复专用镜像格式对比表
| 格式名   | 全称/核心定义                | 技术特性/关联工具                                                                 | 用途与常见场景                                                                 | 优缺点/注意事项                                                                 |
|----------|------------------------------|-----------------------------------------------------------------------------------|------------------------------------------------------------------------------|--------------------------------------------------------------------------------|
| ISO      | 光盘镜像格式（遵循ISO 9660标准） | - 包含完整文件系统结构（模拟光盘分区）；<br>- 支持工具：Rufus、UltraISO、BalenaEtcher、微软MediaCreationTool | 1. **全平台系统安装**：Windows（Win10/11官方镜像均为ISO）、Linux（Ubuntu/CentOS镜像）、macOS（安装镜像需转换为ISO或DMG）；<br>2. **启动介质制作**：写入U盘（UEFI/BIOS均支持）或刻录光盘，直接引导装机；<br>3. **系统备份归档**：将已安装的系统分区打包为ISO，便于跨设备迁移 | ✅ 优势：兼容性极强（所有装机工具支持）、支持UEFI安全启动、可直接挂载查看文件；<br>❌ 缺点：压缩率较低（比GHO/WIM大10%-20%）、不支持增量备份 |
| GHO      | Ghost镜像格式（Norton Ghost专属） | - 按扇区备份分区/硬盘数据；<br>- 支持工具：Symantec Ghost 15、装机员PE（一键Ghost）、DiskGenius | 1. **个人/批量装机**：第三方精简系统（如“纯净版Win10”“游戏专用系统”）多为GHO格式，恢复速度快（10分钟内完成系统部署）；<br>2. **本地系统备份**：将C盘（系统分区）完整备份为GHO，故障时一键还原；<br>3. **老旧设备适配**：对BIOS+MBR机型兼容性极佳，早期PE工具默认支持 | ✅ 优势：压缩率高（比ISO小20%-30%）、恢复速度快、操作简单（小白易上手）；<br>❌ 缺点：<br>- 早期版本不支持GPT分区（新版Ghost 15已兼容）；<br>- 不支持“多系统共存”（一个GHO仅对应单系统）；<br>- 镜像文件易损坏（需校验MD5） |
| WIM      | Windows Imaging Format（微软官方镜像格式） | - 基于文件级备份（非扇区），支持“单文件多版本”；<br>- 关联工具：DISM（微软官方命令）、ImageX、NTLite | 1. **微软原生系统部署**：Win10/11安装包中核心文件“install.wim”（含Home/Pro/Enterprise等多版本，装机时选择对应版本）；<br>2. **系统增量备份**：对已备份的WIM镜像，仅追加更新的文件（适合日常系统备份，节省空间）；<br>3. **企业级部署**：通过微软SCCM（系统中心配置管理器）批量推送WIM镜像，统一装机 | ✅ 优势：支持增量/差异备份、可修改镜像内容（如注入驱动/补丁）、兼容GPT+UEFI、文件级备份（镜像损坏仅丢失单个文件）；<br>❌ 缺点：仅支持Windows系统、恢复速度比GHO慢（文件级校验耗时） |
| ESD      | Electronic Software Download（微软高压缩镜像） | - 基于WIM改进，采用LZMS高压缩算法；<br>- 关联工具：DISM（支持ESD与WIM互转）、7-Zip（解压查看） | 1. **微软系统分发**：微软官网早期Win10/11更新包、“精简版系统”常用ESD（如“Win11 22H2 精简版.esd”）；<br>2. **小容量存储场景**：U盘空间不足时，用ESD减少镜像体积（比WIM小30%+，Win11镜像可从5GB压至3.5GB） | ✅ 优势：压缩率极高（系统镜像体积最小）、支持微软官方签名（安全性高）；<br>❌ 缺点：<br>- 解压/恢复速度慢（比WIM多耗时5-10分钟）；<br>- 部分第三方工具不支持直接恢复（需先转WIM）；<br>- 微软官方ESD可能加密（需解密后使用） |
| VHD/VHDX | 虚拟硬盘镜像格式（微软虚拟存储标准） | - VHD：最大支持2TB，兼容Win7及以上；<br>- VHDX：升级款（支持64TB、4K对齐、日志式存储）；<br>- 关联工具：Windows磁盘管理、Hyper-V、Disk2vhd | 1. **原生启动系统**：Win10/11支持从VHDX启动（将VHDX作为“虚拟系统盘”，无需虚拟机，直接在物理机引导）；<br>2. **系统测试/隔离**：在现有系统中挂载VHDX，安装测试版系统（如Win12预览版），不影响原系统；<br>3. **系统备份迁移**：将旧电脑系统打包为VHDX，直接挂载到新电脑使用（需确保硬件兼容） | ✅ 优势：可直接挂载查看/修改文件（无需完整恢复）、VHDX支持4K硬盘（性能更好）、适合多系统隔离；<br>❌ 缺点：启动速度比物理硬盘慢5%-10%、部分老主板（2015年前）不支持VHDX启动 |
| IMG      | 通用镜像格式（按扇区复制的原始镜像） | - 无固定标准，多为“扇区级完整备份”；<br>- 关联工具：Win32 Disk Imager、BalenaEtcher、dd（Linux命令） | 1. **Linux/嵌入式系统**：树莓派（Raspbian镜像为IMG）、ARM架构设备（如开发板系统）、Linux服务器（CentOS嵌入式版）；<br>2. **老旧系统备份**：Win98/XP等老系统，用IMG备份完整硬盘（含MBR分区表）；<br>3. **特殊设备装机**：工业电脑、路由器（OpenWRT固件多为IMG） | ✅ 优势：对小众架构兼容性好（ARM/MIPS）、支持“裸机恢复”（直接写入硬盘即可启动）；<br>❌ 缺点：<br>- 体积大（按硬盘容量计算，空空间也会备份）；<br>- 不支持Windows系统增量备份；<br>- 需匹配硬件架构（如ARM的IMG不能用于x86电脑） |
